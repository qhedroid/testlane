{
  "name": "testlane",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @testlane/web dev",
    "dev:reset": "bash scripts/reset-web-dev.sh",
    "dev:clean": "RELAY_DEV_CLEAN=1 pnpm dev",
    "build": "pnpm --filter @testlane/web build",
    "start": "pnpm --filter @testlane/web start",
    "db:generate": "pnpm --filter @testlane/db generate",
    "db:wait": "bash scripts/wait-for-mysql.sh",
    "db:migrate": "pnpm db:wait && pnpm --filter @testlane/db migrate",
    "db:studio": "pnpm --filter @testlane/db studio",
    "db:seed": "pnpm db:wait && pnpm --filter @testlane/db seed",
    "db:validate-create-run": "pnpm db:wait && pnpm --filter @testlane/db validate:create-run",
    "db:validate-update-case-result": "pnpm db:wait && pnpm --filter @testlane/db validate:update-case-result",
    "api:validate": "pnpm db:seed && pnpm --filter @testlane/web validate:api",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "pnpm": {
    "overrides": {
      "drizzle-orm": "^0.30.10"
    }
  }
}
