import type { ReactNode } from 'react'
import '@/legacy/app-runs/runs.css'
import '@/components/api-runs/api-runs-tokens.css'

export default function RunsApiLayout({ children }: { children: ReactNode }) {
  return children
}
